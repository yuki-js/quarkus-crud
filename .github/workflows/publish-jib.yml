name: Build and Publish with Jib

on:
  push:
    branches: [ main, copilot/implement-docker-build-publish ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  NORMAL_BASE_IMAGE: gcr.io/distroless/java21-debian12
  DEBUG_BASE_IMAGE: eclipse-temurin:21-jre

jobs:
  prepare:
    name: Prepare build information
    runs-on: ubuntu-latest
    outputs:
      short_sha: ${{ steps.sha.outputs.short_sha }}
      image_base: ${{ steps.image.outputs.image_base }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Generate short SHA
        id: sha
        run: |
          SHORT_SHA=$(git rev-parse --short=7 HEAD)
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "Short SHA: ${SHORT_SHA}"

      - name: Prepare image name
        id: image
        run: |
          IMAGE_BASE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "image_base=${IMAGE_BASE,,}" >> $GITHUB_OUTPUT
          echo "Image base: ${IMAGE_BASE,,}"

  build-and-push-jvm:
    name: Build and push JVM images
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build application
        run: ./gradlew build --no-daemon

      - name: Build and push JVM normal image
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Build and push JVM debug image
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -Djib.from.image=${{ env.DEBUG_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm-debug \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Tag as latest (on main branch)
        if: github.ref == 'refs/heads/main'
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:latest-jvm \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Tag with semver (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          ./gradlew jib --no-daemon \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${VERSION}-jvm \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

  build-and-push-native:
    name: Build and push native images
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '21'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: 'true'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build native executable
        run: |
          ./gradlew build -Dquarkus.package.type=native --no-daemon

      - name: Prepare native executable for Jib
        run: |
          mkdir -p build/jib-native
          NATIVE_RUNNER=$(find build -name '*-runner' -type f ! -path "*/quarkus-app/*" | head -n 1)
          if [ -z "$NATIVE_RUNNER" ]; then
            echo "Native runner not found! Searching in build directory..."
            find build -type f -executable
            exit 1
          fi
          echo "Found native runner: $NATIVE_RUNNER"
          cp "$NATIVE_RUNNER" build/jib-native/quarkus-run
          chmod +x build/jib-native/quarkus-run
          ls -lh build/jib-native/

      - name: Build and push native normal image
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -PnativeBuild \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Build and push native debug image
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -PnativeBuild \
            -Djib.from.image=${{ env.DEBUG_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native-debug \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Tag as latest (on main branch)
        if: github.ref == 'refs/heads/main'
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./gradlew jib --no-daemon \
            -PnativeBuild \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:latest-native \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

      - name: Tag with semver (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          REGISTRY_USERNAME: ${{ github.actor }}
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          ./gradlew jib --no-daemon \
            -PnativeBuild \
            -Djib.from.image=${{ env.NORMAL_BASE_IMAGE }} \
            -Djib.to.image=${{ needs.prepare.outputs.image_base }}:${VERSION}-native \
            -Djib.to.auth.username=${{ github.actor }} \
            -Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}

  verify-images:
    name: Verify built images
    runs-on: ubuntu-latest
    needs: [prepare, build-and-push-jvm, build-and-push-native]
    steps:
      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Pull and verify JVM normal image
        run: |
          echo "Pulling JVM normal image..."
          docker pull ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm
          echo "Verifying image exists..."
          docker images | grep quarkus-crud
          echo "Starting container..."
          docker run -d --name jvm-test -p 8080:8080 ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm
          echo "Waiting for application to start..."
          sleep 10
          echo "Checking container status..."
          docker ps -a
          docker logs jvm-test
          echo "Stopping container..."
          docker stop jvm-test || true
          docker rm jvm-test || true

      - name: Pull and verify JVM debug image
        run: |
          echo "Pulling JVM debug image..."
          docker pull ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm-debug
          echo "Verifying image exists..."
          docker images | grep quarkus-crud
          echo "Starting container..."
          docker run -d --name jvm-debug-test -p 8080:8080 ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm-debug
          echo "Waiting for application to start..."
          sleep 10
          echo "Checking container status..."
          docker ps -a
          docker logs jvm-debug-test
          echo "Stopping container..."
          docker stop jvm-debug-test || true
          docker rm jvm-debug-test || true

      - name: Pull and verify Native normal image
        run: |
          echo "Pulling Native normal image..."
          docker pull ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native
          echo "Verifying image exists..."
          docker images | grep quarkus-crud
          echo "Starting container..."
          docker run -d --name native-test -p 8080:8080 ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native
          echo "Waiting for application to start (native starts faster)..."
          sleep 5
          echo "Checking container status..."
          docker ps -a
          docker logs native-test
          echo "Stopping container..."
          docker stop native-test || true
          docker rm native-test || true

      - name: Pull and verify Native debug image
        run: |
          echo "Pulling Native debug image..."
          docker pull ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native-debug
          echo "Verifying image exists..."
          docker images | grep quarkus-crud
          echo "Starting container..."
          docker run -d --name native-debug-test -p 8080:8080 ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native-debug
          echo "Waiting for application to start..."
          sleep 5
          echo "Checking container status..."
          docker ps -a
          docker logs native-debug-test
          echo "Stopping container..."
          docker stop native-debug-test || true
          docker rm native-debug-test || true

      - name: Summary
        run: |
          echo "✅ All 4 image variants verified successfully!"
          echo "Images pulled and launched:"
          echo "  - ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm"
          echo "  - ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-jvm-debug"
          echo "  - ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native"
          echo "  - ${{ needs.prepare.outputs.image_base }}:${{ needs.prepare.outputs.short_sha }}-native-debug"
